{
  "rules": {
    ".read": true,
    "messages": {
      "$message": {
        ".write": true,
        ".validate": "newData.hasChildren(['name','color','text','createdAt']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 10 && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 500 && newData.child('color').isString() && newData.child('createdAt').isNumber()"
      }
    }
  }
}